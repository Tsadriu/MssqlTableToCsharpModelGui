<Window x:Class="MssqlTableToCsharpModelGui.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="MSSQL Table to C# model" Height="800" Width="600">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Label>Stored Connection Strings</Label>
        <ListBox Grid.Row="1" Grid.Column="0" x:Name="listConnectionString" />
        <StackPanel Grid.Row="1" Grid.Column="1" Margin="5,0,0,0">
            <StackPanel Margin="5,0,0,0">
                <TextBox x:Name="textConnectionString" />
                <Button x:Name="buttonAddConnectionString" Margin="0,5,0,0" Click="ButtonAddConnectionString_OnClick">Add connection string</Button>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
